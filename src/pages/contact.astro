---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Contact" description="Get in touch with Muzammil Mahmud">
  <article class="container" style="padding-top: 3rem; padding-bottom: 4rem;">
    <h1 style="font-size: 2.25rem; font-weight: 700; margin: 0 0 1.5rem 0; letter-spacing: -0.025em;">
      Contact
    </h1>
    <p class="lead" style="color: var(--color-text-muted); margin-bottom: 2rem;">
      Reach out for collaborations, questions, or just to say hi.
    </p>

    <section class="contact-section">
      <h2 style="font-size: 1.125rem; font-weight: 600; margin: 0 0 1rem 0;">
        Email
      </h2>
      <a href="mailto:your.email@example.com">your.email@example.com</a>
    </section>

    <section class="contact-section">
      <h2 style="font-size: 1.125rem; font-weight: 600; margin: 0 0 1rem 0;">
        Social
      </h2>
      <ul class="social-links">
        <li><a href="https://github.com/muzammilmahmud" target="_blank" rel="noopener noreferrer">GitHub</a></li>
        <li><a href="https://linkedin.com" target="_blank" rel="noopener noreferrer">LinkedIn</a></li>
        <li><a href="https://twitter.com" target="_blank" rel="noopener noreferrer">Twitter / X</a></li>
      </ul>
    </section>

    <section class="contact-section subscribe-section">
      <h2 style="font-size: 1.125rem; font-weight: 600; margin: 0 0 1rem 0;">
        Subscribe
      </h2>
      <p style="color: var(--color-text-muted); margin: 0 0 1rem 0; font-size: 0.9375rem;">
        Get updates when I publish new posts. No spam.
      </p>
      <!-- Buttondown embed: replace 'your-username' with your Buttondown username -->
      <form action="https://buttondown.com/api/emails/embed-subscribe/your-username" method="post" target="_blank" class="subscribe-form">
        <input type="email" name="email" placeholder="you@example.com" required class="subscribe-input" />
        <input type="hidden" name="embeddable" value="true" />
        <button type="submit" class="subscribe-btn">Subscribe</button>
      </form>
      <p style="font-size: 0.75rem; color: var(--color-text-muted); margin-top: 0.75rem;">
        Powered by <a href="https://buttondown.com" target="_blank" rel="noopener noreferrer">Buttondown</a>. Free for up to 100 subscribers.
      </p>
    </section>

    <section class="contact-section">
      <h2 style="font-size: 1.125rem; font-weight: 600; margin: 0 0 1rem 0;">
        Contact form
      </h2>
      <p style="color: var(--color-text-muted); margin: 0 0 1rem 0; font-size: 0.9375rem;">
        Or use the form below. Submissions go to Formspree (set up at formspree.io).
      </p>
      <form action="https://formspree.io/f/YOUR_FORM_ID" method="POST" class="contact-form">
        <div class="form-group">
          <label for="name">Name</label>
          <input type="text" id="name" name="name" required />
        </div>
        <div class="form-group">
          <label for="email">Email</label>
          <input type="email" id="email" name="email" required />
        </div>
        <div class="form-group">
          <label for="message">Message</label>
          <textarea id="message" name="message" rows="4" required></textarea>
        </div>
        <button type="submit" class="submit-btn">Send</button>
      </form>
    </section>
  </article>
</Layout>

<style>
  .contact-section {
    margin-bottom: 2.5rem;
  }

  .social-links {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    gap: 1.5rem;
  }

  .subscribe-form,
  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    max-width: 400px;
  }

  .subscribe-form {
    flex-direction: row;
    flex-wrap: wrap;
  }

  .subscribe-input {
    flex: 1;
    min-width: 200px;
    padding: 0.625rem 1rem;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius);
    color: var(--color-text);
    font-family: inherit;
  }

  .subscribe-input::placeholder {
    color: var(--color-text-muted);
  }

  .subscribe-btn {
    padding: 0.625rem 1.25rem;
    background: var(--color-accent);
    color: var(--color-bg);
    border: none;
    border-radius: var(--radius);
    font-weight: 500;
    cursor: pointer;
  }

  .subscribe-btn:hover {
    background: var(--color-accent-hover);
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: 0.375rem;
  }

  .form-group label {
    font-size: 0.875rem;
    color: var(--color-text-muted);
  }

  .form-group input,
  .form-group textarea {
    padding: 0.625rem 1rem;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius);
    color: var(--color-text);
    font-family: inherit;
  }

  .form-group textarea {
    resize: vertical;
  }

  .submit-btn {
    padding: 0.625rem 1.25rem;
    background: var(--color-accent);
    color: var(--color-bg);
    border: none;
    border-radius: var(--radius);
    font-weight: 500;
    cursor: pointer;
    width: fit-content;
  }

  .submit-btn:hover {
    background: var(--color-accent-hover);
  }
</style>
